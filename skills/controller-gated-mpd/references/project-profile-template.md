# Project Profile Template

Record durable project facts that are safe to reuse across waves:

- repository and trunk;
- canonical verification workflow identity;
- gate catalog;
- path-floor policy;
- hot zones;
- merge method defaults;
- repository-specific ownership constraints.

Per-wave frozen SHA, lane branches, dependencies, authorization, and transport belong in `wave.json`.
